/**
 * This is a Repository template class
 * This class works as a bridge between the application and the data source (e.g. the database) 
 */

import Template from '../models/Template';

class TemplateRepository {
    constructor() {}
}

export default TemplateRepository;